diff --git a/src/virtio/vtest/vtest_protocol.h b/src/virtio/vtest/vtest_protocol.h
index c8a1bbf..c622c5b 100644
--- a/src/virtio/vtest/vtest_protocol.h
+++ b/src/virtio/vtest/vtest_protocol.h
@@ -98,6 +98,22 @@
 #define VCMD_SYNC_EXPORT_SYNC_FILE 39
 /* import a dmabuf fd as a blob resource attached to the context */
 #define VCMD_RESOURCE_IMPORT_BLOB 40
+/* 41 = VCMD_RESOURCE_ALLOC_GPU (gralloc wrapper only, unused here) */
+/* get the context's shared DRM timeline syncobj
+ * (reply: status + 2 fds: syncobj, render node)
+ */
+#define VCMD_TIMELINE_GET 42
+/* submit_cmd2 whose fence also materializes a timeline point:
+ * payload = {point_lo, point_hi} followed by a regular submit_cmd2 payload
+ */
+#define VCMD_SUBMIT_CMD2_TIMELINE 43
+/* import a sync_file fd (via SCM_RIGHTS) as the temporary payload of a venus
+ * semaphore, so the host GPU waits it instead of the guest CPU:
+ * payload = {device_id_lo, device_id_hi, semaphore_id_lo, semaphore_id_hi};
+ * reply = {status} — the roundtrip orders the import before any subsequent
+ * ring submission that waits on the semaphore
+ */
+#define VCMD_SEMAPHORE_IMPORT_SYNC_FD 44
 
 #define VCMD_RES_CREATE_SIZE 10
 #define VCMD_RES_CREATE_RES_HANDLE 0 /* must be 0 since protocol version 3 */
@@ -170,6 +186,10 @@ enum vcmd_param  {
    VCMD_PARAM_HAS_TIMELINE_SYNCOBJ = 2,
    /* server supports VCMD_SYNC_EXPORT_SYNC_FILE for venus sync points */
    VCMD_PARAM_HAS_VENUS_SYNC_FD = 3,
+   /* server supports VCMD_TIMELINE_GET + VCMD_SUBMIT_CMD2_TIMELINE */
+   VCMD_PARAM_HAS_TIMELINE_FENCE = 4,
+   /* server supports VCMD_SEMAPHORE_IMPORT_SYNC_FD */
+   VCMD_PARAM_HAS_SEMAPHORE_IMPORT = 5,
 };
 #define VCMD_GET_PARAM_SIZE 1
 #define VCMD_GET_PARAM_PARAM 0
diff --git a/src/virtio/vulkan/meson.build b/src/virtio/vulkan/meson.build
index 02a3d37..ec053df 100644
--- a/src/virtio/vulkan/meson.build
+++ b/src/virtio/vulkan/meson.build
@@ -79,6 +79,7 @@ libvn_files = files(
   'vn_feedback.c',
   'vn_host_copy.c',
   'vn_icd.c',
+  'vn_astc_emu.c',
   'vn_image.c',
   'vn_instance.c',
   'vn_physical_device.c',
diff --git a/src/virtio/vulkan/vn_android.c b/src/virtio/vulkan/vn_android.c
index eaa82bc..9d8025c 100644
--- a/src/virtio/vulkan/vn_android.c
+++ b/src/virtio/vulkan/vn_android.c
@@ -8,6 +8,7 @@
  * Copyright © 2016 Bas Nieuwenhuizen
  */
 
+#include "util/log.h"
 #include "vn_android.h"
 
 #include <dlfcn.h>
@@ -153,17 +154,42 @@ vn_android_ahb_image_init(struct vn_device *dev,
    VkImageDrmFormatModifierExplicitCreateInfoEXT mod_info;
    VkSubresourceLayout layouts[VN_MAX_PLANES];
    result = vk_android_get_ahb_layout(ahb, &mod_info, layouts, VN_MAX_PLANES);
-   if (result != VK_SUCCESS)
+   if (result != VK_SUCCESS) {
+      mesa_loge("NVWD: ahb_image_init: get_ahb_layout failed %d", result);
       return result;
+   }
    __vk_append_struct(create_info, &mod_info);
 
+   /* Older ANGLE picks legacy LINEAR tiling for CPU-visible AHBs, but venus
+    * imports dma_bufs via explicit modifiers only; the modifier chain above
+    * requires DRM_FORMAT_MODIFIER tiling or the host rejects the image.
+    */
+   create_info->tiling = VK_IMAGE_TILING_DRM_FORMAT_MODIFIER_EXT;
+
+   /* ANGLE tacks INPUT_ATTACHMENT onto every sampled AHB image "just in
+    * case", but NVIDIA rejects the dedicated import for LINEAR-modifier
+    * images with that usage (vkAllocateMemory -> INVALID_EXTERNAL_HANDLE),
+    * which crashed SurfaceFlinger on every screen capture. Nothing input-
+    * attaches EGLImage siblings; drop the bit for linear imports.
+    */
+   if (mod_info.drmFormatModifier == 0 /* DRM_FORMAT_MOD_LINEAR */)
+      create_info->usage &= ~VK_IMAGE_USAGE_INPUT_ATTACHMENT_BIT;
+
    VkExternalMemoryImageCreateInfo external_info = {
       .sType = VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_IMAGE_CREATE_INFO,
       .handleTypes = VK_EXTERNAL_MEMORY_HANDLE_TYPE_DMA_BUF_BIT_EXT,
    };
    __vk_append_struct(create_info, &external_info);
 
-   return vn_image_init_deferred(dev, create_info, img);
+   result = vn_image_init_deferred(dev, create_info, img);
+   if (result != VK_SUCCESS) {
+      mesa_loge("NVWD: ahb_image_init: init_deferred failed %d "
+                "(%ux%u fmt %u usage 0x%x mod 0x%" PRIx64 ")",
+                result, create_info->extent.width, create_info->extent.height,
+                create_info->format, create_info->usage,
+                mod_info.drmFormatModifier);
+   }
+   return result;
 }
 
 VkResult
@@ -177,13 +203,17 @@ vn_android_device_import_ahb(struct vn_device *dev,
    const native_handle_t *handle =
       AHardwareBuffer_getNativeHandle(mem_vk->ahardware_buffer);
    int dma_buf_fd = vn_android_gralloc_get_dma_buf_fd(handle);
-   if (dma_buf_fd < 0)
+   if (dma_buf_fd < 0) {
+      mesa_loge("NVWD: import_ahb: no dma_buf fd in handle");
       return VK_ERROR_INVALID_EXTERNAL_HANDLE;
+   }
 
    uint32_t mem_type_bits = 0;
    result = vn_get_memory_dma_buf_properties(dev, dma_buf_fd, &mem_type_bits);
-   if (result != VK_SUCCESS)
+   if (result != VK_SUCCESS) {
+      mesa_loge("NVWD: import_ahb: dma_buf_properties failed %d", result);
       return result;
+   }
 
    const VkMemoryDedicatedAllocateInfo *dedicated_info =
       vk_find_struct_const(alloc_info->pNext, MEMORY_DEDICATED_ALLOCATE_INFO);
@@ -192,8 +222,10 @@ vn_android_device_import_ahb(struct vn_device *dev,
    if (dedicated_info && dedicated_info->image != VK_NULL_HANDLE) {
       struct vn_image *img = vn_image_from_handle(dedicated_info->image);
       result = vn_android_ahb_image_init(dev, mem_vk->ahardware_buffer, img);
-      if (result != VK_SUCCESS)
+      if (result != VK_SUCCESS) {
+         mesa_loge("NVWD: import_ahb: ahb_image_init failed %d", result);
          return result;
+      }
 
       mem_reqs = img->requirements[0].memory.memoryRequirements;
       mem_reqs.memoryTypeBits &= mem_type_bits;
@@ -206,11 +238,21 @@ vn_android_device_import_ahb(struct vn_device *dev,
       mem_reqs.memoryTypeBits = mem_type_bits;
    }
 
-   if (!mem_reqs.memoryTypeBits)
+   if (!mem_reqs.memoryTypeBits) {
+      mesa_loge("NVWD: import_ahb: empty memoryTypeBits");
       return VK_ERROR_INVALID_EXTERNAL_HANDLE;
+   }
 
-   if (!((1 << mem_vk->memory_type_index) & mem_reqs.memoryTypeBits))
-      mem_vk->memory_type_index = ffs(mem_reqs.memoryTypeBits) - 1;
+   /* Always import into the LOWEST type of the real host mask, ignoring the
+    * app's pick: the app chooses against UMA-stamped flags (host-visible
+    * types advertise DEVICE_LOCAL for it), but NVIDIA rejects actual dmabuf
+    * imports into some advertised types (host-visible-cached observed
+    * failing on 610.172), and with async allocation that failure surfaces
+    * later as a fatal CS error on vkBindImageMemory2. Type 0 (plain sysmem)
+    * always imports; CPU access goes through the dma_buf fd mmap, never
+    * vkMapMemory, so the type's host-visibility is irrelevant.
+    */
+   mem_vk->memory_type_index = ffs(mem_reqs.memoryTypeBits) - 1;
 
    mem_vk->size = mem_reqs.size;
 
@@ -239,6 +281,9 @@ vn_android_device_import_ahb(struct vn_device *dev,
    result =
       vn_device_memory_import_dma_buf(dev, mem, &local_alloc_info, dup_fd);
    if (result != VK_SUCCESS) {
+      mesa_loge("NVWD: import_ahb: import_dma_buf failed %d (size %" PRIu64
+                " type %u)",
+                result, mem_vk->size, mem_vk->memory_type_index);
       close(dup_fd);
       return result;
    }
diff --git a/src/virtio/vulkan/vn_astc_emu.c b/src/virtio/vulkan/vn_astc_emu.c
new file mode 100644
index 0000000..c6ae5ac
--- /dev/null
+++ b/src/virtio/vulkan/vn_astc_emu.c
@@ -0,0 +1,448 @@
+/*
+ * Copyright 2026 waydroid-nvidia project
+ * SPDX-License-Identifier: MIT
+ */
+
+#include "vn_astc_emu.h"
+
+#include "vk_format.h"
+#include "vk_texcompress_astc.h"
+
+#include "vn_command_buffer.h"
+#include "vn_device.h"
+#include "vn_image.h"
+#include "vn_physical_device.h"
+
+/* All entrypoints used below are venus' own: they encode into the command
+ * stream / ring exactly like app calls, so the decode pass is transparent to
+ * the host.
+ */
+
+bool
+vn_astc_emu_format(VkFormat format)
+{
+   return vk_texcompress_astc_emulation_format(format) != VK_FORMAT_UNDEFINED;
+}
+
+VkFormat
+vn_astc_emu_get_rgba_format(VkFormat format)
+{
+   return vk_texcompress_astc_emulation_format(format);
+}
+
+void
+vn_astc_emu_device_init(struct vn_device *dev)
+{
+   simple_mtx_init(&dev->astc_emu.mutex, mtx_plain);
+   dev->astc_emu.texcompress = NULL;
+}
+
+void
+vn_astc_emu_device_fini(struct vn_device *dev)
+{
+   if (dev->astc_emu.texcompress) {
+      vk_texcompress_astc_finish(&dev->base.vk, &dev->base.vk.alloc,
+                                 dev->astc_emu.texcompress);
+      dev->astc_emu.texcompress = NULL;
+   }
+   simple_mtx_destroy(&dev->astc_emu.mutex);
+}
+
+static struct vk_texcompress_astc_state *
+vn_astc_emu_get_state(struct vn_device *dev)
+{
+   struct vn_astc_emu *emu = &dev->astc_emu;
+
+   if (likely(emu->texcompress))
+      return emu->texcompress;
+
+   simple_mtx_lock(&emu->mutex);
+   if (!emu->texcompress) {
+      struct vk_texcompress_astc_state *state = NULL;
+      VkResult result = vk_texcompress_astc_init(
+         &dev->base.vk, &dev->base.vk.alloc, VK_NULL_HANDLE, &state,
+         (struct vk_texcompress_astc_params){
+            .luts_alignment = 64,
+            .luts_memory_flags = VK_MEMORY_PROPERTY_HOST_VISIBLE_BIT |
+                                 VK_MEMORY_PROPERTY_HOST_COHERENT_BIT,
+         });
+      if (result == VK_SUCCESS)
+         emu->texcompress = state;
+      else
+         vn_log(dev->instance, "ASTC emu: vk_texcompress_astc_init failed");
+   }
+   simple_mtx_unlock(&emu->mutex);
+
+   return emu->texcompress;
+}
+
+static struct vn_astc_emu_temp_obj *
+vn_astc_emu_alloc_temp(struct vn_command_buffer *cmd)
+{
+   const VkAllocationCallbacks *alloc = &cmd->base.vk.pool->alloc;
+   struct vn_astc_emu_temp_obj *temp =
+      vk_zalloc(alloc, sizeof(*temp), VN_DEFAULT_ALIGN,
+                VK_SYSTEM_ALLOCATION_SCOPE_OBJECT);
+   if (!temp)
+      return NULL;
+
+   list_addtail(&temp->head, &cmd->builder.astc_temp_objs);
+   return temp;
+}
+
+void
+vn_astc_emu_cmd_reset(struct vn_command_buffer *cmd)
+{
+   struct vn_device *dev = vn_device_from_vk(cmd->base.vk.pool->base.device);
+   VkDevice dev_handle = vn_device_to_handle(dev);
+   const VkAllocationCallbacks *alloc = &cmd->base.vk.pool->alloc;
+
+   list_for_each_entry_safe(struct vn_astc_emu_temp_obj, temp,
+                            &cmd->builder.astc_temp_objs, head) {
+      if (temp->src_view != VK_NULL_HANDLE)
+         vn_DestroyImageView(dev_handle, temp->src_view, NULL);
+      if (temp->dst_view != VK_NULL_HANDLE)
+         vn_DestroyImageView(dev_handle, temp->dst_view, NULL);
+      if (temp->image != VK_NULL_HANDLE)
+         vn_DestroyImage(dev_handle, temp->image, NULL);
+      if (temp->memory != VK_NULL_HANDLE)
+         vn_FreeMemory(dev_handle, temp->memory, NULL);
+      list_del(&temp->head);
+      vk_free(alloc, temp);
+   }
+}
+
+static uint32_t
+vn_astc_emu_find_memory_type(struct vn_device *dev,
+                             uint32_t type_bits)
+{
+   const VkPhysicalDeviceMemoryProperties *props =
+      &dev->physical_device->memory_properties;
+
+   /* prefer DEVICE_LOCAL */
+   for (uint32_t i = 0; i < props->memoryTypeCount; i++) {
+      if ((type_bits & (1u << i)) &&
+          (props->memoryTypes[i].propertyFlags &
+           VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT))
+         return i;
+   }
+   for (uint32_t i = 0; i < props->memoryTypeCount; i++) {
+      if (type_bits & (1u << i))
+         return i;
+   }
+   return 0;
+}
+
+/* Create the transient block image holding raw ASTC blocks for one copy
+ * region: R32G32B32A32_UINT, one texel per block, single mip.
+ */
+static VkResult
+vn_astc_emu_create_block_image(struct vn_command_buffer *cmd,
+                               struct vn_astc_emu_temp_obj *temp,
+                               VkExtent2D block_extent,
+                               uint32_t layer_count)
+{
+   struct vn_device *dev = vn_device_from_vk(cmd->base.vk.pool->base.device);
+   VkDevice dev_handle = vn_device_to_handle(dev);
+   VkResult result;
+
+   const VkImageCreateInfo img_info = {
+      .sType = VK_STRUCTURE_TYPE_IMAGE_CREATE_INFO,
+      .imageType = VK_IMAGE_TYPE_2D,
+      .format = VK_FORMAT_R32G32B32A32_UINT,
+      .extent = { block_extent.width, block_extent.height, 1 },
+      .mipLevels = 1,
+      .arrayLayers = layer_count,
+      .samples = VK_SAMPLE_COUNT_1_BIT,
+      .tiling = VK_IMAGE_TILING_OPTIMAL,
+      .usage = VK_IMAGE_USAGE_TRANSFER_DST_BIT | VK_IMAGE_USAGE_SAMPLED_BIT,
+      .sharingMode = VK_SHARING_MODE_EXCLUSIVE,
+      .initialLayout = VK_IMAGE_LAYOUT_UNDEFINED,
+   };
+   result = vn_CreateImage(dev_handle, &img_info, NULL, &temp->image);
+   if (result != VK_SUCCESS)
+      return result;
+
+   const VkImageMemoryRequirementsInfo2 reqs_info = {
+      .sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_REQUIREMENTS_INFO_2,
+      .image = temp->image,
+   };
+   VkMemoryRequirements2 reqs = {
+      .sType = VK_STRUCTURE_TYPE_MEMORY_REQUIREMENTS_2,
+   };
+   vn_GetImageMemoryRequirements2(dev_handle, &reqs_info, &reqs);
+
+   const VkMemoryAllocateInfo mem_info = {
+      .sType = VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_INFO,
+      .allocationSize = reqs.memoryRequirements.size,
+      .memoryTypeIndex = vn_astc_emu_find_memory_type(
+         dev, reqs.memoryRequirements.memoryTypeBits),
+   };
+   result = vn_AllocateMemory(dev_handle, &mem_info, NULL, &temp->memory);
+   if (result != VK_SUCCESS)
+      return result;
+
+   const VkBindImageMemoryInfo bind_info = {
+      .sType = VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_INFO,
+      .image = temp->image,
+      .memory = temp->memory,
+      .memoryOffset = 0,
+   };
+   /* vn only implements the 2 variant; host-side this is 1.1 core which the
+    * renderer resolves independently of the app's device version */
+   return vn_BindImageMemory2(dev_handle, 1, &bind_info);
+}
+
+static void
+vn_astc_emu_save_compute_state(struct vn_command_buffer *cmd)
+{
+   /* the shadow is maintained by vn_Cmd* hooks; nothing to snapshot */
+}
+
+static void
+vn_astc_emu_restore_compute_state(struct vn_command_buffer *cmd)
+{
+   VkCommandBuffer cmd_handle = vn_command_buffer_to_handle(cmd);
+   const struct vn_astc_emu_saved_compute_state *s =
+      &cmd->builder.astc_saved_compute;
+
+   if (s->pipeline != VK_NULL_HANDLE) {
+      vn_CmdBindPipeline(cmd_handle, VK_PIPELINE_BIND_POINT_COMPUTE,
+                         s->pipeline);
+   }
+   if (s->has_desc_set) {
+      vn_CmdBindDescriptorSets(cmd_handle, VK_PIPELINE_BIND_POINT_COMPUTE,
+                               s->desc_layout, s->desc_first_set, 1,
+                               &s->desc_set, 0, NULL);
+   }
+   if (s->has_push_const) {
+      vn_CmdPushConstants(cmd_handle, s->push_const_layout,
+                          s->push_const_stages, s->push_const_offset,
+                          s->push_const_size, s->push_const_data);
+   }
+}
+
+static void
+vn_astc_emu_decode_region(struct vn_command_buffer *cmd,
+                          struct vn_image *img,
+                          const VkBufferImageCopy2 *region,
+                          VkBuffer src_buffer,
+                          VkImageLayout dst_layout)
+{
+   struct vn_device *dev = vn_device_from_vk(cmd->base.vk.pool->base.device);
+   VkDevice dev_handle = vn_device_to_handle(dev);
+   VkCommandBuffer cmd_handle = vn_command_buffer_to_handle(cmd);
+   VkImage img_handle = vn_image_to_handle(img);
+   const VkFormat astc_format = img->astc_emu_format;
+   const uint32_t bw = vk_format_get_blockwidth(astc_format);
+   const uint32_t bh = vk_format_get_blockheight(astc_format);
+
+   struct vk_texcompress_astc_state *astc = vn_astc_emu_get_state(dev);
+   if (!astc)
+      return;
+
+   VkPipeline pipeline = vk_texcompress_astc_get_decode_pipeline(
+      &dev->base.vk, (VkAllocationCallbacks *)&dev->base.vk.alloc, astc,
+      VK_NULL_HANDLE, astc_format);
+   if (pipeline == VK_NULL_HANDLE) {
+      vn_log(dev->instance, "ASTC emu: no decode pipeline");
+      return;
+   }
+
+   /* compressed-copy imageOffset is block aligned per spec */
+   const uint32_t block_off_x = region->imageOffset.x / bw;
+   const uint32_t block_off_y = region->imageOffset.y / bh;
+   const VkExtent2D region_blocks = {
+      DIV_ROUND_UP(region->imageExtent.width, bw),
+      DIV_ROUND_UP(region->imageExtent.height, bh),
+   };
+   /* temp image spans [0, offset+extent) blocks so that src and dst share
+    * one coordinate space (the decode shader adds the pushed block offset
+    * to both reads and writes)
+    */
+   const VkExtent2D block_extent = {
+      block_off_x + region_blocks.width,
+      block_off_y + region_blocks.height,
+   };
+   const uint32_t layer_count = region->imageSubresource.layerCount;
+
+   struct vn_astc_emu_temp_obj *temp = vn_astc_emu_alloc_temp(cmd);
+   if (!temp)
+      return;
+
+   if (vn_astc_emu_create_block_image(cmd, temp, block_extent,
+                                      layer_count) != VK_SUCCESS)
+      return;
+
+   /* 1. block image UNDEFINED -> TRANSFER_DST */
+   const VkImageMemoryBarrier to_xfer = {
+      .sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER,
+      .srcAccessMask = 0,
+      .dstAccessMask = VK_ACCESS_TRANSFER_WRITE_BIT,
+      .oldLayout = VK_IMAGE_LAYOUT_UNDEFINED,
+      .newLayout = VK_IMAGE_LAYOUT_TRANSFER_DST_OPTIMAL,
+      .srcQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+      .dstQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+      .image = temp->image,
+      .subresourceRange = { VK_IMAGE_ASPECT_COLOR_BIT, 0, 1, 0,
+                            layer_count },
+   };
+   vn_CmdPipelineBarrier(cmd_handle, VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT,
+                         VK_PIPELINE_STAGE_TRANSFER_BIT, 0, 0, NULL, 0, NULL,
+                         1, &to_xfer);
+
+   /* 2. upload raw blocks: buffer layout of ASTC (16B blocks) matches
+    * R32G32B32A32 texels one to one
+    */
+   /* use the 1.0 command: the app's device may predate 1.3 and the host
+    * proc table would have no vkCmdCopyBufferToImage2 for it
+    */
+   const VkBufferImageCopy block_region = {
+      .bufferOffset = region->bufferOffset,
+      .bufferRowLength = DIV_ROUND_UP(region->bufferRowLength, bw),
+      .bufferImageHeight = DIV_ROUND_UP(region->bufferImageHeight, bh),
+      .imageSubresource = { VK_IMAGE_ASPECT_COLOR_BIT, 0, 0, layer_count },
+      .imageOffset = { (int32_t)block_off_x, (int32_t)block_off_y, 0 },
+      .imageExtent = { region_blocks.width, region_blocks.height, 1 },
+   };
+   /* temp image is not emulated: falls through to the plain encode */
+   vn_CmdCopyBufferToImage(cmd_handle, src_buffer, temp->image,
+                           VK_IMAGE_LAYOUT_TRANSFER_DST_OPTIMAL, 1,
+                           &block_region);
+
+   /* 3. block image -> shader read; dst RGBA region -> GENERAL */
+   const VkImageMemoryBarrier pre_decode[2] = {
+      {
+         .sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER,
+         .srcAccessMask = VK_ACCESS_TRANSFER_WRITE_BIT,
+         .dstAccessMask = VK_ACCESS_SHADER_READ_BIT,
+         .oldLayout = VK_IMAGE_LAYOUT_TRANSFER_DST_OPTIMAL,
+         .newLayout = VK_IMAGE_LAYOUT_GENERAL,
+         .srcQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+         .dstQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+         .image = temp->image,
+         .subresourceRange = { VK_IMAGE_ASPECT_COLOR_BIT, 0, 1, 0,
+                               layer_count },
+      },
+      {
+         .sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER,
+         .srcAccessMask = VK_ACCESS_TRANSFER_WRITE_BIT,
+         .dstAccessMask = VK_ACCESS_SHADER_WRITE_BIT,
+         .oldLayout = dst_layout,
+         .newLayout = VK_IMAGE_LAYOUT_GENERAL,
+         .srcQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+         .dstQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+         .image = img_handle,
+         .subresourceRange = { VK_IMAGE_ASPECT_COLOR_BIT,
+                               region->imageSubresource.mipLevel, 1,
+                               region->imageSubresource.baseArrayLayer,
+                               layer_count },
+      },
+   };
+   vn_CmdPipelineBarrier(cmd_handle, VK_PIPELINE_STAGE_TRANSFER_BIT,
+                         VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, 0, 0, NULL, 0,
+                         NULL, 2, pre_decode);
+
+   /* 4. views */
+   const VkImageViewCreateInfo src_view_info = {
+      .sType = VK_STRUCTURE_TYPE_IMAGE_VIEW_CREATE_INFO,
+      .image = temp->image,
+      .viewType = VK_IMAGE_VIEW_TYPE_2D_ARRAY,
+      .format = VK_FORMAT_R32G32B32A32_UINT,
+      .subresourceRange = { VK_IMAGE_ASPECT_COLOR_BIT, 0, 1, 0,
+                            layer_count },
+   };
+   if (vn_CreateImageView(dev_handle, &src_view_info, NULL,
+                          &temp->src_view) != VK_SUCCESS)
+      return;
+
+   const VkImageViewCreateInfo dst_view_info = {
+      .sType = VK_STRUCTURE_TYPE_IMAGE_VIEW_CREATE_INFO,
+      .pNext = &(VkImageViewUsageCreateInfo){
+         .sType = VK_STRUCTURE_TYPE_IMAGE_VIEW_USAGE_CREATE_INFO,
+         .usage = VK_IMAGE_USAGE_STORAGE_BIT,
+      },
+      .image = img_handle,
+      .viewType = VK_IMAGE_VIEW_TYPE_2D_ARRAY,
+      .format = VK_FORMAT_R8G8B8A8_UINT,
+      .subresourceRange = { VK_IMAGE_ASPECT_COLOR_BIT,
+                            region->imageSubresource.mipLevel, 1,
+                            region->imageSubresource.baseArrayLayer,
+                            layer_count },
+   };
+   if (vn_CreateImageView(dev_handle, &dst_view_info, NULL,
+                          &temp->dst_view) != VK_SUCCESS)
+      return;
+
+   /* 5. bind + push descriptors + push constants + dispatch */
+   vn_CmdBindPipeline(cmd_handle, VK_PIPELINE_BIND_POINT_COMPUTE, pipeline);
+
+   struct vk_texcompress_astc_write_descriptor_set writes;
+   vk_texcompress_astc_fill_write_descriptor_sets(
+      astc, &writes, temp->src_view, VK_IMAGE_LAYOUT_GENERAL, temp->dst_view,
+      astc_format);
+   vn_CmdPushDescriptorSet(cmd_handle, VK_PIPELINE_BIND_POINT_COMPUTE,
+                           astc->p_layout, 0,
+                           ARRAY_SIZE(writes.descriptor_set),
+                           writes.descriptor_set);
+
+   /* shader convention (see radv_meta_astc_decode.c): block coords of the
+    * copy start, texel coords of the end, is_3d
+    */
+   const uint32_t push_const[] = {
+      block_off_x,
+      block_off_y,
+      region->imageOffset.x + region->imageExtent.width,
+      region->imageOffset.y + region->imageExtent.height,
+      false, /* no 3D (emulation rejects 3D ASTC images) */
+   };
+   vn_CmdPushConstants(cmd_handle, astc->p_layout,
+                       VK_SHADER_STAGE_COMPUTE_BIT, 0, sizeof(push_const),
+                       push_const);
+
+   /* each workgroup handles 2x2 blocks */
+   vn_CmdDispatch(cmd_handle, DIV_ROUND_UP(region_blocks.width, 2),
+                  DIV_ROUND_UP(region_blocks.height, 2), layer_count);
+
+   /* 6. dst RGBA back to the app's layout */
+   const VkImageMemoryBarrier post_decode = {
+      .sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER,
+      .srcAccessMask = VK_ACCESS_SHADER_WRITE_BIT,
+      .dstAccessMask = VK_ACCESS_TRANSFER_WRITE_BIT |
+                       VK_ACCESS_TRANSFER_READ_BIT |
+                       VK_ACCESS_SHADER_READ_BIT,
+      .oldLayout = VK_IMAGE_LAYOUT_GENERAL,
+      .newLayout = dst_layout,
+      .srcQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+      .dstQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+      .image = img_handle,
+      .subresourceRange = { VK_IMAGE_ASPECT_COLOR_BIT,
+                            region->imageSubresource.mipLevel, 1,
+                            region->imageSubresource.baseArrayLayer,
+                            layer_count },
+   };
+   vn_CmdPipelineBarrier(cmd_handle, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
+                         VK_PIPELINE_STAGE_ALL_COMMANDS_BIT, 0, 0, NULL, 0,
+                         NULL, 1, &post_decode);
+}
+
+bool
+vn_astc_emu_cmd_copy_buffer_to_image(struct vn_command_buffer *cmd,
+                                     const VkCopyBufferToImageInfo2 *info)
+{
+   struct vn_image *img = vn_image_from_handle(info->dstImage);
+
+   if (likely(!img->astc_emulated))
+      return false;
+
+   vn_astc_emu_save_compute_state(cmd);
+
+   for (uint32_t i = 0; i < info->regionCount; i++) {
+      vn_astc_emu_decode_region(cmd, img, &info->pRegions[i],
+                                info->srcBuffer, info->dstImageLayout);
+   }
+
+   vn_astc_emu_restore_compute_state(cmd);
+
+   return true;
+}
diff --git a/src/virtio/vulkan/vn_astc_emu.h b/src/virtio/vulkan/vn_astc_emu.h
new file mode 100644
index 0000000..0b6fade
--- /dev/null
+++ b/src/virtio/vulkan/vn_astc_emu.h
@@ -0,0 +1,74 @@
+/*
+ * Copyright 2026 waydroid-nvidia project
+ * SPDX-License-Identifier: MIT
+ *
+ * ASTC LDR emulation for venus on hosts without textureCompressionASTC_LDR
+ * (desktop NVIDIA). The app-visible image is backed by a host RGBA image;
+ * compressed uploads are staged into a transient R32G32B32A32_UINT "block"
+ * image (one texel per ASTC block) and decoded into the RGBA image with the
+ * common vk_texcompress_astc compute pipelines, all recorded inline into the
+ * app's command buffer.
+ */
+
+#ifndef VN_ASTC_EMU_H
+#define VN_ASTC_EMU_H
+
+#include "vn_common.h"
+
+struct vk_texcompress_astc_state;
+struct vn_command_buffer;
+struct vn_device;
+struct vn_image;
+
+struct vn_astc_emu {
+   simple_mtx_t mutex;
+   /* lazily initialized on first compressed upload */
+   struct vk_texcompress_astc_state *texcompress;
+};
+
+/* transient objects recorded into a command buffer; freed at cmd reset */
+struct vn_astc_emu_temp_obj {
+   VkImage image;
+   VkDeviceMemory memory;
+   VkImageView src_view;
+   VkImageView dst_view;
+   struct list_head head;
+};
+
+/* last-known compute state to restore after injected decode dispatches */
+struct vn_astc_emu_saved_compute_state {
+   VkPipeline pipeline;
+   VkPipelineLayout desc_layout;
+   uint32_t desc_first_set;
+   VkDescriptorSet desc_set;
+   bool has_desc_set;
+   VkPipelineLayout push_const_layout;
+   VkShaderStageFlags push_const_stages;
+   uint32_t push_const_offset;
+   uint32_t push_const_size;
+   uint8_t push_const_data[256];
+   bool has_push_const;
+};
+
+void
+vn_astc_emu_device_init(struct vn_device *dev);
+
+void
+vn_astc_emu_device_fini(struct vn_device *dev);
+
+bool
+vn_astc_emu_format(VkFormat format);
+
+VkFormat
+vn_astc_emu_get_rgba_format(VkFormat format);
+
+/* returns true if the copy was fully handled by the emulation path */
+bool
+vn_astc_emu_cmd_copy_buffer_to_image(
+   struct vn_command_buffer *cmd,
+   const VkCopyBufferToImageInfo2 *info);
+
+void
+vn_astc_emu_cmd_reset(struct vn_command_buffer *cmd);
+
+#endif /* VN_ASTC_EMU_H */
diff --git a/src/virtio/vulkan/vn_command_buffer.c b/src/virtio/vulkan/vn_command_buffer.c
index e7be2ff..6eebd5e 100644
--- a/src/virtio/vulkan/vn_command_buffer.c
+++ b/src/virtio/vulkan/vn_command_buffer.c
@@ -10,6 +10,8 @@
 
 #include "vn_command_buffer.h"
 
+#include "vn_astc_emu.h"
+
 #include "venus-protocol/vn_protocol_driver_command_buffer.h"
 #include "venus-protocol/vn_protocol_driver_command_pool.h"
 #include "vk_synchronization.h"
@@ -734,10 +736,12 @@ vn_cmd_reset(struct vn_command_buffer *cmd)
    cmd->base.vk.state = MESA_VK_COMMAND_BUFFER_STATE_INITIAL;
 
    /* reset cmd builder */
+   vn_astc_emu_cmd_reset(cmd);
    vk_free(&cmd->base.vk.pool->alloc, cmd->builder.present_src_images);
    vn_cmd_pool_free_query_records(cmd_pool, &cmd->builder.query_records);
    memset(&cmd->builder, 0, sizeof(cmd->builder));
    list_inithead(&cmd->builder.query_records);
+   list_inithead(&cmd->builder.astc_temp_objs);
 
    if (cmd->linked_qfb_cmd) {
       vn_query_feedback_cmd_free(cmd->linked_qfb_cmd);
@@ -865,6 +869,7 @@ vn_AllocateCommandBuffers(VkDevice device,
                          VN_CS_ENCODER_STORAGE_SHMEM_POOL, 16 * 1024);
 
       list_inithead(&cmd->builder.query_records);
+      list_inithead(&cmd->builder.astc_temp_objs);
 
       VkCommandBuffer cmd_handle = vn_command_buffer_to_handle(cmd);
       pCommandBuffers[i] = cmd_handle;
@@ -1150,6 +1155,12 @@ vn_CmdBindPipeline(VkCommandBuffer commandBuffer,
                    VkPipelineBindPoint pipelineBindPoint,
                    VkPipeline pipeline)
 {
+   if (pipelineBindPoint == VK_PIPELINE_BIND_POINT_COMPUTE) {
+      struct vn_command_buffer *cmd =
+         vn_command_buffer_from_handle(commandBuffer);
+      cmd->builder.astc_saved_compute.pipeline = pipeline;
+   }
+
    VN_CMD_ENQUEUE(vkCmdBindPipeline, commandBuffer, pipelineBindPoint,
                   pipeline);
 }
@@ -1243,6 +1254,21 @@ vn_CmdBindDescriptorSets(VkCommandBuffer commandBuffer,
                          uint32_t dynamicOffsetCount,
                          const uint32_t *pDynamicOffsets)
 {
+   /* shadow compute set 0 without dynamic offsets for the ASTC decode
+    * state restore (the decode pass only clobbers set 0)
+    */
+   if (pipelineBindPoint == VK_PIPELINE_BIND_POINT_COMPUTE &&
+       firstSet == 0 && descriptorSetCount >= 1 && !dynamicOffsetCount) {
+      struct vn_command_buffer *cmd =
+         vn_command_buffer_from_handle(commandBuffer);
+      struct vn_astc_emu_saved_compute_state *sh =
+         &cmd->builder.astc_saved_compute;
+      sh->desc_layout = layout;
+      sh->desc_first_set = 0;
+      sh->desc_set = pDescriptorSets[0];
+      sh->has_desc_set = true;
+   }
+
    VN_CMD_ENQUEUE(vkCmdBindDescriptorSets, commandBuffer, pipelineBindPoint,
                   layout, firstSet, descriptorSetCount, pDescriptorSets,
                   dynamicOffsetCount, pDynamicOffsets);
@@ -1464,6 +1490,35 @@ vn_CmdCopyBufferToImage(VkCommandBuffer commandBuffer,
                         uint32_t regionCount,
                         const VkBufferImageCopy *pRegions)
 {
+   struct vn_image *dst_img = vn_image_from_handle(dstImage);
+   if (unlikely(dst_img->astc_emulated)) {
+      struct vn_command_buffer *cmd =
+         vn_command_buffer_from_handle(commandBuffer);
+      STACK_ARRAY(VkBufferImageCopy2, regions2, regionCount);
+      for (uint32_t i = 0; i < regionCount; i++) {
+         regions2[i] = (VkBufferImageCopy2){
+            .sType = VK_STRUCTURE_TYPE_BUFFER_IMAGE_COPY_2,
+            .bufferOffset = pRegions[i].bufferOffset,
+            .bufferRowLength = pRegions[i].bufferRowLength,
+            .bufferImageHeight = pRegions[i].bufferImageHeight,
+            .imageSubresource = pRegions[i].imageSubresource,
+            .imageOffset = pRegions[i].imageOffset,
+            .imageExtent = pRegions[i].imageExtent,
+         };
+      }
+      const VkCopyBufferToImageInfo2 info = {
+         .sType = VK_STRUCTURE_TYPE_COPY_BUFFER_TO_IMAGE_INFO_2,
+         .srcBuffer = srcBuffer,
+         .dstImage = dstImage,
+         .dstImageLayout = dstImageLayout,
+         .regionCount = regionCount,
+         .pRegions = regions2,
+      };
+      vn_astc_emu_cmd_copy_buffer_to_image(cmd, &info);
+      STACK_ARRAY_FINISH(regions2);
+      return;
+   }
+
    VN_CMD_ENQUEUE(vkCmdCopyBufferToImage, commandBuffer, srcBuffer, dstImage,
                   dstImageLayout, regionCount, pRegions);
 }
@@ -1473,6 +1528,12 @@ vn_CmdCopyBufferToImage2(
    VkCommandBuffer commandBuffer,
    const VkCopyBufferToImageInfo2 *pCopyBufferToImageInfo)
 {
+   struct vn_command_buffer *cmd =
+      vn_command_buffer_from_handle(commandBuffer);
+   if (unlikely(vn_astc_emu_cmd_copy_buffer_to_image(
+          cmd, pCopyBufferToImageInfo)))
+      return;
+
    VN_CMD_ENQUEUE(vkCmdCopyBufferToImage2, commandBuffer,
                   pCopyBufferToImageInfo);
 }
@@ -1854,6 +1915,19 @@ vn_CmdPushConstants(VkCommandBuffer commandBuffer,
                     uint32_t size,
                     const void *pValues)
 {
+   if ((stageFlags & VK_SHADER_STAGE_COMPUTE_BIT) && size <= 256) {
+      struct vn_command_buffer *cmd =
+         vn_command_buffer_from_handle(commandBuffer);
+      struct vn_astc_emu_saved_compute_state *sh =
+         &cmd->builder.astc_saved_compute;
+      sh->push_const_layout = layout;
+      sh->push_const_stages = stageFlags;
+      sh->push_const_offset = offset;
+      sh->push_const_size = size;
+      memcpy(sh->push_const_data, pValues, size);
+      sh->has_push_const = true;
+   }
+
    VN_CMD_ENQUEUE(vkCmdPushConstants, commandBuffer, layout, stageFlags,
                   offset, size, pValues);
 }
diff --git a/src/virtio/vulkan/vn_command_buffer.h b/src/virtio/vulkan/vn_command_buffer.h
index a5099e9..3b2ffd7 100644
--- a/src/virtio/vulkan/vn_command_buffer.h
+++ b/src/virtio/vulkan/vn_command_buffer.h
@@ -13,6 +13,8 @@
 
 #include "vn_common.h"
 
+#include "vn_astc_emu.h"
+
 #include "vn_cs.h"
 
 struct vn_command_pool {
@@ -65,6 +67,10 @@ struct vn_command_buffer_builder {
    bool is_simultaneous;
    /* track the recorded queries and resets */
    struct list_head query_records;
+
+   /* ASTC emulation: transient decode objects + compute state shadow */
+   struct list_head astc_temp_objs;
+   struct vn_astc_emu_saved_compute_state astc_saved_compute;
 };
 
 struct vn_query_feedback_cmd;
diff --git a/src/virtio/vulkan/vn_device.c b/src/virtio/vulkan/vn_device.c
index f7a1f1c..5f2c0ff 100644
--- a/src/virtio/vulkan/vn_device.c
+++ b/src/virtio/vulkan/vn_device.c
@@ -252,6 +252,14 @@ vn_device_fix_create_info(const struct vn_device *dev,
    uint32_t extra_count = 0;
    uint32_t block_count = 0;
 
+   /* ASTC emulation records vkCmdPushDescriptorSet(KHR) into app command
+    * buffers; the host proc table only resolves it when the device enables
+    * the extension (or the host is 1.4+)
+    */
+   if (physical_dev->emu_astc_ldr && renderer_exts->KHR_push_descriptor &&
+       !app_exts->KHR_push_descriptor)
+      extra_exts[extra_count++] = VK_KHR_PUSH_DESCRIPTOR_EXTENSION_NAME;
+
    /* fix for WSI (treat AHB as WSI extension for simplicity) */
    const bool has_wsi =
       app_exts->KHR_swapchain || app_exts->ANDROID_native_buffer ||
@@ -496,6 +504,38 @@ vn_device_init(struct vn_device *dev,
       dev->device_mask = (1 << group->physicalDeviceCount) - 1;
 
    VkDeviceCreateInfo final_create_info = *create_info;
+
+   /* the host lacks textureCompressionASTC_LDR when emulating; strip it */
+   VkPhysicalDeviceFeatures astc_local_feats;
+   VkPhysicalDeviceFeatures2 astc_local_feats2;
+   if (physical_dev->emu_astc_ldr) {
+      if (final_create_info.pEnabledFeatures &&
+          final_create_info.pEnabledFeatures->textureCompressionASTC_LDR) {
+         astc_local_feats = *final_create_info.pEnabledFeatures;
+         astc_local_feats.textureCompressionASTC_LDR = VK_FALSE;
+         final_create_info.pEnabledFeatures = &astc_local_feats;
+      }
+      const VkPhysicalDeviceFeatures2 *feats2 = vk_find_struct_const(
+         final_create_info.pNext, PHYSICAL_DEVICE_FEATURES_2);
+      if (feats2 && feats2->features.textureCompressionASTC_LDR) {
+         /* shallow-copy the struct and splice it into a copied chain head */
+         astc_local_feats2 = *feats2;
+         astc_local_feats2.features.textureCompressionASTC_LDR = VK_FALSE;
+         /* walk the copy of the chain and replace the pointer */
+         if (final_create_info.pNext == feats2) {
+            final_create_info.pNext = &astc_local_feats2;
+         } else {
+            /* features2 deeper in the chain: patch the const chain by
+             * pointing our copy's predecessor at it is not possible without
+             * copying the whole chain; fall back to mutating through the
+             * cast (chain structs are writable app memory per spec usage)
+             */
+            ((VkPhysicalDeviceFeatures2 *)feats2)
+               ->features.textureCompressionASTC_LDR = VK_FALSE;
+         }
+      }
+   }
+
    STACK_ARRAY(VkDeviceQueueCreateInfo, queue_infos,
                create_info->queueCreateInfoCount);
    for (uint32_t i = 0; i < create_info->queueCreateInfoCount; i++) {
@@ -553,6 +593,10 @@ vn_device_init(struct vn_device *dev,
    simple_mtx_init(&dev->mutex, mtx_plain);
    list_inithead(&dev->chains);
 
+   simple_mtx_init(&dev->dma_buf_props_cache.mutex, mtx_plain);
+
+   vn_astc_emu_device_init(dev);
+
    return VK_SUCCESS;
 
 out_feedback_cmd_pools_fini:
@@ -641,6 +685,9 @@ vn_DestroyDevice(VkDevice device, const VkAllocationCallbacks *pAllocator)
 
    assert(list_is_empty(&dev->chains));
    simple_mtx_destroy(&dev->mutex);
+   simple_mtx_destroy(&dev->dma_buf_props_cache.mutex);
+
+   vn_astc_emu_device_fini(dev);
 
    vn_image_reqs_cache_fini(dev);
    vn_buffer_reqs_cache_fini(dev);
diff --git a/src/virtio/vulkan/vn_device.h b/src/virtio/vulkan/vn_device.h
index e871564..f4c7cc5 100644
--- a/src/virtio/vulkan/vn_device.h
+++ b/src/virtio/vulkan/vn_device.h
@@ -13,11 +13,31 @@
 
 #include "vn_common.h"
 
+#include "vn_astc_emu.h"
+
 #include "vn_buffer.h"
 #include "vn_device_memory.h"
 #include "vn_feedback.h"
 #include "vn_image.h"
 
+/* Cache of dma_buf memoryTypeBits keyed by the dma_buf's inode.  Each
+ * uncached vn_get_memory_dma_buf_properties costs three renderer roundtrips
+ * (bo import, ring roundtrip, ring call) and Android re-queries the same
+ * gralloc buffers every frame (AHB wrapping in ANGLE/HWUI).  dma_buf inode
+ * numbers come from a monotonic counter and are never reused, and a given
+ * buffer's memoryTypeBits are immutable, so cache hits are always sound.
+ */
+#define VN_DMA_BUF_PROPS_CACHE_SIZE 64
+struct vn_dma_buf_props_cache {
+   simple_mtx_t mutex;
+   struct {
+      uint64_t ino;
+      uint32_t mem_type_bits;
+   } entries[VN_DMA_BUF_PROPS_CACHE_SIZE];
+   uint32_t count; /* valid entries */
+   uint32_t next;  /* round-robin replacement cursor */
+};
+
 struct vn_device {
    struct vn_device_base base;
 
@@ -42,8 +62,11 @@ struct vn_device {
    struct vn_queue *queues;
    uint32_t queue_count;
 
+   struct vn_astc_emu astc_emu;
+
    struct vn_buffer_reqs_cache buffer_reqs_cache;
    struct vn_image_reqs_cache image_reqs_cache;
+   struct vn_dma_buf_props_cache dma_buf_props_cache;
 
    bool has_sync2;
 
diff --git a/src/virtio/vulkan/vn_device_memory.c b/src/virtio/vulkan/vn_device_memory.c
index 8b6eb4c..38e1511 100644
--- a/src/virtio/vulkan/vn_device_memory.c
+++ b/src/virtio/vulkan/vn_device_memory.c
@@ -8,6 +8,9 @@
  * Copyright © 2016 Bas Nieuwenhuizen
  */
 
+#include <sys/stat.h>
+
+#include "util/log.h"
 #include "vn_device_memory.h"
 
 #include "venus-protocol/vn_protocol_driver_device_memory.h"
@@ -374,6 +377,41 @@ vn_AllocateMemory(VkDevice device,
 {
    struct vn_device *dev = vn_device_from_handle(device);
 
+   /* Steer plain host-visible allocations to a HOST_CACHED type of the same
+    * heap. Over vtest, CPU access goes through mmap of an exported dma_buf;
+    * on NVIDIA that mapping is ~1 GB/s write / 0.2 GB/s read for the
+    * uncached-coherent type but ~31 / 7.5 GB/s for the cached one
+    * (tests/nvmembench.c). Both are HOST_COHERENT, so no flush-semantics
+    * change; skip imports and dedicated allocations whose memoryTypeBits we
+    * cannot see here.
+    */
+   VkMemoryAllocateInfo steered_info;
+   if (!vk_find_struct_const(pAllocateInfo->pNext, IMPORT_MEMORY_FD_INFO_KHR) &&
+       !vk_find_struct_const(pAllocateInfo->pNext,
+                             IMPORT_ANDROID_HARDWARE_BUFFER_INFO_ANDROID) &&
+       !vk_find_struct_const(pAllocateInfo->pNext,
+                             MEMORY_DEDICATED_ALLOCATE_INFO)) {
+      const VkPhysicalDeviceMemoryProperties *props =
+         &dev->physical_device->memory_properties;
+      const uint32_t idx = pAllocateInfo->memoryTypeIndex;
+      const VkMemoryType *cur = &props->memoryTypes[idx];
+      if ((cur->propertyFlags & VK_MEMORY_PROPERTY_HOST_VISIBLE_BIT) &&
+          !(cur->propertyFlags & VK_MEMORY_PROPERTY_HOST_CACHED_BIT)) {
+         for (uint32_t i = 0; i < props->memoryTypeCount; i++) {
+            const VkMemoryType *cand = &props->memoryTypes[i];
+            if (cand->heapIndex == cur->heapIndex &&
+                (cand->propertyFlags &
+                 (cur->propertyFlags | VK_MEMORY_PROPERTY_HOST_CACHED_BIT)) ==
+                   (cur->propertyFlags | VK_MEMORY_PROPERTY_HOST_CACHED_BIT)) {
+               steered_info = *pAllocateInfo;
+               steered_info.memoryTypeIndex = i;
+               pAllocateInfo = &steered_info;
+               break;
+            }
+         }
+      }
+   }
+
    struct vn_device_memory *mem = vk_device_memory_create(
       &dev->base.vk, pAllocateInfo, pAllocator, sizeof(*mem));
    if (!mem)
@@ -584,6 +622,54 @@ vn_GetMemoryFdKHR(VkDevice device,
    return VK_SUCCESS;
 }
 
+/* dma_buf inodes come from a monotonic counter (never reused) and a buffer's
+ * memoryTypeBits are immutable, so a cache hit is always sound.  Returns
+ * true on a hit; on a miss *out_ino is the fd's inode, or 0 when it could
+ * not be determined (don't cache).
+ */
+static bool
+vn_dma_buf_props_cache_lookup(struct vn_device *dev,
+                              int fd,
+                              uint32_t *out_mem_type_bits,
+                              uint64_t *out_ino)
+{
+   struct vn_dma_buf_props_cache *cache = &dev->dma_buf_props_cache;
+   struct stat st;
+
+   *out_ino = 0;
+   if (fstat(fd, &st))
+      return false;
+
+   simple_mtx_lock(&cache->mutex);
+   for (uint32_t i = 0; i < cache->count; i++) {
+      if (cache->entries[i].ino == st.st_ino) {
+         *out_mem_type_bits = cache->entries[i].mem_type_bits;
+         simple_mtx_unlock(&cache->mutex);
+         return true;
+      }
+   }
+   simple_mtx_unlock(&cache->mutex);
+
+   *out_ino = st.st_ino;
+   return false;
+}
+
+static void
+vn_dma_buf_props_cache_insert(struct vn_device *dev,
+                              uint64_t ino,
+                              uint32_t mem_type_bits)
+{
+   struct vn_dma_buf_props_cache *cache = &dev->dma_buf_props_cache;
+
+   simple_mtx_lock(&cache->mutex);
+   cache->entries[cache->next].ino = ino;
+   cache->entries[cache->next].mem_type_bits = mem_type_bits;
+   cache->next = (cache->next + 1) % VN_DMA_BUF_PROPS_CACHE_SIZE;
+   if (cache->count < VN_DMA_BUF_PROPS_CACHE_SIZE)
+      cache->count++;
+   simple_mtx_unlock(&cache->mutex);
+}
+
 VkResult
 vn_get_memory_dma_buf_properties(struct vn_device *dev,
                                  int fd,
@@ -591,6 +677,13 @@ vn_get_memory_dma_buf_properties(struct vn_device *dev,
 {
    VkDevice device = vn_device_to_handle(dev);
 
+   /* Android wraps the same gralloc buffers as AHBs every frame; the
+    * uncached query below costs three renderer roundtrips per call.
+    */
+   uint64_t ino;
+   if (vn_dma_buf_props_cache_lookup(dev, fd, out_mem_type_bits, &ino))
+      return VK_SUCCESS;
+
    struct vn_renderer_bo *bo;
    VkResult result = vn_renderer_bo_create_from_dma_buf(
       dev->renderer, 0 /* size */, fd, 0 /* flags */, &bo);
@@ -614,6 +707,9 @@ vn_get_memory_dma_buf_properties(struct vn_device *dev,
 
    *out_mem_type_bits = props.memoryTypeBits;
 
+   if (ino)
+      vn_dma_buf_props_cache_insert(dev, ino, props.memoryTypeBits);
+
    return VK_SUCCESS;
 }
 
diff --git a/src/virtio/vulkan/vn_image.c b/src/virtio/vulkan/vn_image.c
index 5d4f916..c669c7c 100644
--- a/src/virtio/vulkan/vn_image.c
+++ b/src/virtio/vulkan/vn_image.c
@@ -8,8 +8,11 @@
  * Copyright © 2016 Bas Nieuwenhuizen
  */
 
+#include "util/log.h"
 #include "vn_image.h"
 
+#include "vn_astc_emu.h"
+
 #include "venus-protocol/vn_protocol_driver_image.h"
 #include "venus-protocol/vn_protocol_driver_image_view.h"
 #include "venus-protocol/vn_protocol_driver_sampler.h"
@@ -606,7 +609,35 @@ vn_CreateImage(VkDevice device,
             pCreateInfo, renderer_handle_type, &local_info);
       }
 
+      /* ASTC emulation: back the image with an RGBA host image; uploads are
+       * decoded by compute (vn_astc_emu.c). Only plain 2D non-external
+       * images are emulated.
+       */
+      VkImageCreateInfo astc_local_info;
+      const VkFormat astc_rgba_format =
+         dev->physical_device->emu_astc_ldr && !external_info &&
+               pCreateInfo->imageType == VK_IMAGE_TYPE_2D
+            ? vn_astc_emu_get_rgba_format(pCreateInfo->format)
+            : VK_FORMAT_UNDEFINED;
+      const VkFormat astc_orig_format = pCreateInfo->format;
+      const VkImageUsageFlags astc_app_usage = pCreateInfo->usage;
+      if (astc_rgba_format != VK_FORMAT_UNDEFINED) {
+         astc_local_info = *pCreateInfo;
+         astc_local_info.format = astc_rgba_format;
+         astc_local_info.usage |= VK_IMAGE_USAGE_STORAGE_BIT;
+         astc_local_info.flags &= ~VK_IMAGE_CREATE_BLOCK_TEXEL_VIEW_COMPATIBLE_BIT;
+         astc_local_info.flags |= VK_IMAGE_CREATE_MUTABLE_FORMAT_BIT |
+                                  VK_IMAGE_CREATE_EXTENDED_USAGE_BIT;
+         pCreateInfo = &astc_local_info;
+      }
+
       result = vn_image_create(dev, pCreateInfo, alloc, &img);
+
+      if (result == VK_SUCCESS && astc_rgba_format != VK_FORMAT_UNDEFINED) {
+         img->astc_emulated = true;
+         img->astc_emu_format = astc_orig_format;
+         img->astc_app_usage = astc_app_usage;
+      }
    }
 
    if (result != VK_SUCCESS)
@@ -820,6 +851,27 @@ vn_CreateImageView(VkDevice device,
       assert(pCreateInfo->format != VK_FORMAT_UNDEFINED);
    }
 
+   /* ASTC emulation: the host image is RGBA */
+   VkImageViewUsageCreateInfo astc_usage_info;
+   if (img->astc_emulated &&
+       vn_astc_emu_format(pCreateInfo->format)) {
+      local_info = *pCreateInfo;
+      local_info.format = vn_astc_emu_get_rgba_format(pCreateInfo->format);
+      /* the image gained STORAGE for the decode pass; app views (esp. SRGB)
+       * must not inherit it
+       */
+      if (!vk_find_struct_const(pCreateInfo->pNext,
+                                IMAGE_VIEW_USAGE_CREATE_INFO)) {
+         astc_usage_info = (VkImageViewUsageCreateInfo){
+            .sType = VK_STRUCTURE_TYPE_IMAGE_VIEW_USAGE_CREATE_INFO,
+            .pNext = local_info.pNext,
+            .usage = img->astc_app_usage,
+         };
+         local_info.pNext = &astc_usage_info;
+      }
+      pCreateInfo = &local_info;
+   }
+
    struct vn_image_view *view =
       vk_zalloc(alloc, sizeof(*view), VN_DEFAULT_ALIGN,
                 VK_SYSTEM_ALLOCATION_SCOPE_OBJECT);
diff --git a/src/virtio/vulkan/vn_image.h b/src/virtio/vulkan/vn_image.h
index 74bdeed..43af197 100644
--- a/src/virtio/vulkan/vn_image.h
+++ b/src/virtio/vulkan/vn_image.h
@@ -59,6 +59,12 @@ struct vn_image {
       /* memory backing the prime blit dst buffer */
       struct vn_device_memory *blit_mem;
    } wsi;
+
+   /* ASTC emulation (see vn_astc_emu.c): host image is RGBA, uploads are
+    * decoded by compute */
+   bool astc_emulated;
+   VkFormat astc_emu_format;
+   VkImageUsageFlags astc_app_usage;
 };
 VK_DEFINE_NONDISP_HANDLE_CASTS(vn_image,
                                base.vk.base,
diff --git a/src/virtio/vulkan/vn_physical_device.c b/src/virtio/vulkan/vn_physical_device.c
index 7ee58ac..9d254e1 100644
--- a/src/virtio/vulkan/vn_physical_device.c
+++ b/src/virtio/vulkan/vn_physical_device.c
@@ -8,8 +8,13 @@
  * Copyright © 2016 Bas Nieuwenhuizen
  */
 
+#include "util/log.h"
 #include "vn_physical_device.h"
 
+#include "vn_astc_emu.h"
+
+#include "util/u_process.h"
+
 #include <stdio.h>
 
 #include "git_sha1.h"
@@ -431,6 +436,16 @@ vn_physical_device_init_features(struct vn_physical_device *physical_dev)
     */
    feats->deviceMemoryReport = true;
 
+   /* ASTC LDR emulation on hosts without it (desktop GPUs): uploads are
+    * decoded to RGBA by compute (vn_astc_emu.c). Android requires the
+    * feature, so Vulkan-native games blindly rely on it.
+    */
+   if (!feats->textureCompressionASTC_LDR &&
+       !debug_get_bool_option("VN_NO_ASTC_EMU", false)) {
+      physical_dev->emu_astc_ldr = true;
+      feats->textureCompressionASTC_LDR = true;
+   }
+
    /* VK_EXT_map_memory_placed */
    if (supported_exts->EXT_map_memory_placed) {
       feats->memoryMapPlaced = true;
@@ -1018,6 +1033,26 @@ vn_physical_device_init_memory_properties(
       props->memoryTypes[first_coherent].propertyFlags |=
          VK_MEMORY_PROPERTY_HOST_CACHED_BIT;
    }
+
+   /* Advertise HOST_VISIBLE memory types as DEVICE_LOCAL too (UMA-style,
+    * like integrated GPUs). Android clients (ANGLE AHB imports, HWUI)
+    * demand DEVICE_LOCAL for gralloc memory, but our CPU-mappable gralloc
+    * buffers import only into host-visible sysmem types on NVIDIA — their
+    * import masks always include a host-visible type, which this covers.
+    *
+    * Deliberately NOT stamped on non-host-visible sysmem types (NVIDIA
+    * type 0, heap 1, flags NONE): it precedes the real VRAM type, and
+    * first-fit DEVICE_LOCAL pickers would place every render target and
+    * texture in system RAM — measured 60-240x slower (PCIe-bound; see
+    * tests/gpuload.c and STATE.md 2026-07-12).
+    */
+   for (uint32_t i = 0; i < props->memoryTypeCount; i++) {
+      VkMemoryPropertyFlags *flags = &props->memoryTypes[i].propertyFlags;
+      if (*flags & VK_MEMORY_PROPERTY_HOST_VISIBLE_BIT)
+         *flags |= VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT;
+   }
+   for (uint32_t i = 0; i < props->memoryHeapCount; i++)
+      props->memoryHeaps[i].flags |= VK_MEMORY_HEAP_DEVICE_LOCAL_BIT;
 }
 
 static void
@@ -2283,6 +2318,26 @@ vn_GetPhysicalDeviceFormatProperties2(VkPhysicalDevice physicalDevice,
       }
    }
 
+   /* ASTC LDR emulation: sampled + transfer dst, optimal tiling only */
+   if (physical_dev->emu_astc_ldr && vn_astc_emu_format(format)) {
+      const VkFormatFeatureFlags astc_features =
+         VK_FORMAT_FEATURE_SAMPLED_IMAGE_BIT |
+         VK_FORMAT_FEATURE_SAMPLED_IMAGE_FILTER_LINEAR_BIT |
+         VK_FORMAT_FEATURE_TRANSFER_SRC_BIT |
+         VK_FORMAT_FEATURE_TRANSFER_DST_BIT;
+      *props = (VkFormatProperties){
+         .optimalTilingFeatures = astc_features,
+      };
+      if (props3) {
+         props3->linearTilingFeatures = 0;
+         props3->optimalTilingFeatures = (VkFormatFeatureFlags2)astc_features;
+         props3->bufferFeatures = 0;
+      }
+      if (srpq)
+         *srpq = VK_FALSE;
+      return;
+   }
+
    vn_call_vkGetPhysicalDeviceFormatProperties2(ring, physicalDevice, format,
                                                 pFormatProperties);
    vn_sanitize_format_properties(format, props, props3);
@@ -2713,9 +2768,18 @@ vn_get_ahb_image_props(struct vn_physical_device *physical_dev,
    /* Only advertise support for optimal tiling since venus is unable to
     * detiling if the AHB allocated outside is tiled. Internally venus always
     * overrides the tiling to VK_IMAGE_TILING_DRM_FORMAT_MODIFIER_EXT.
+    *
+    * Also accept LINEAR: older ANGLE requests legacy LINEAR tiling for
+    * CPU-visible AHBs and silently rejects the AHB when this query fails,
+    * killing SurfaceFlinger capture paths. The tiling is overridden to
+    * DRM_FORMAT_MODIFIER internally either way (see vn_android_ahb_image_init).
     */
-   if (info->tiling != VK_IMAGE_TILING_OPTIMAL)
+   if (info->tiling != VK_IMAGE_TILING_OPTIMAL &&
+       info->tiling != VK_IMAGE_TILING_LINEAR) {
+      mesa_loge("NVWD: ahb_image_props: reject tiling %d (fmt %u usage 0x%x)",
+                info->tiling, info->format, info->usage);
       return VK_ERROR_FORMAT_NOT_SUPPORTED;
+   }
 
    /* No need to support AHB + VK_EXT_separate_stencil_usage. */
    if (vk_find_struct_const(info->pNext, IMAGE_STENCIL_USAGE_CREATE_INFO))
@@ -2734,8 +2798,12 @@ vn_get_ahb_image_props(struct vn_physical_device *physical_dev,
     */
    VkResult result = vk_android_get_ahb_image_properties(
       vn_physical_device_to_handle(physical_dev), info, props);
-   if (result != VK_SUCCESS)
+   if (result != VK_SUCCESS) {
+      mesa_loge("NVWD: ahb_image_props: common helper failed %d "
+                "(fmt %u tiling %d usage 0x%x)",
+                result, info->format, info->tiling, info->usage);
       return result;
+   }
 
    /* Now properly fill all the default props. */
 
@@ -2853,6 +2921,30 @@ vn_GetPhysicalDeviceImageFormatProperties2(
       pImageFormatInfo = &local_info.format;
    }
 
+   /* ASTC LDR emulation: answer with the backing RGBA image's limits by
+    * rewriting the query the same way vn_CreateImage rewrites creation
+    */
+   struct vn_physical_device_image_format_info astc_local_info;
+   if (physical_dev->emu_astc_ldr &&
+       vn_astc_emu_format(pImageFormatInfo->format)) {
+      if (external_info || pImageFormatInfo->tiling != VK_IMAGE_TILING_OPTIMAL ||
+          pImageFormatInfo->type == VK_IMAGE_TYPE_3D ||
+          (pImageFormatInfo->usage &
+           ~(VK_IMAGE_USAGE_SAMPLED_BIT | VK_IMAGE_USAGE_TRANSFER_SRC_BIT |
+             VK_IMAGE_USAGE_TRANSFER_DST_BIT))) {
+         return vn_error(physical_dev->instance,
+                         VK_ERROR_FORMAT_NOT_SUPPORTED);
+      }
+      astc_local_info.format = *pImageFormatInfo;
+      astc_local_info.format.pNext = NULL;
+      astc_local_info.format.format =
+         vn_astc_emu_get_rgba_format(pImageFormatInfo->format);
+      astc_local_info.format.usage |= VK_IMAGE_USAGE_STORAGE_BIT;
+      astc_local_info.format.flags |= VK_IMAGE_CREATE_MUTABLE_FORMAT_BIT |
+                                      VK_IMAGE_CREATE_EXTENDED_USAGE_BIT;
+      pImageFormatInfo = &astc_local_info.format;
+   }
+
    /* Check if image format props is in the cache. */
    uint8_t key[BLAKE3_KEY_LEN] = { 0 };
    const bool cacheable = vn_image_get_image_format_key(
@@ -2880,6 +2972,14 @@ vn_GetPhysicalDeviceImageFormatProperties2(
       }
    }
 
+   if (result != VK_SUCCESS) {
+      mesa_loge("NVWD: ImgFmtProps2 FAIL %d: fmt %u tiling %d usage 0x%x "
+                "flags 0x%x ext 0x%x",
+                result, pImageFormatInfo->format, pImageFormatInfo->tiling,
+                pImageFormatInfo->usage, pImageFormatInfo->flags,
+                external_info ? external_info->handleType : 0);
+   }
+
    return vn_result(physical_dev->instance, result);
 }
 
diff --git a/src/virtio/vulkan/vn_physical_device.h b/src/virtio/vulkan/vn_physical_device.h
index c33f3cf..2c0c156 100644
--- a/src/virtio/vulkan/vn_physical_device.h
+++ b/src/virtio/vulkan/vn_physical_device.h
@@ -113,6 +113,9 @@ struct vn_physical_device {
 
    VkPhysicalDeviceMemoryProperties memory_properties;
 
+   /* host lacks textureCompressionASTC_LDR; venus emulates it */
+   bool emu_astc_ldr;
+
    struct {
       VkExternalMemoryHandleTypeFlagBits renderer_handle_type;
       VkExternalMemoryHandleTypeFlags supported_handle_types;
diff --git a/src/virtio/vulkan/vn_queue.c b/src/virtio/vulkan/vn_queue.c
index 4c337a3..3957600 100644
--- a/src/virtio/vulkan/vn_queue.c
+++ b/src/virtio/vulkan/vn_queue.c
@@ -383,6 +383,9 @@ vn_fix_device_group_cmd_count(struct vn_queue_submission *submit,
 static bool
 vn_semaphore_wait_external(struct vn_device *dev, struct vn_semaphore *sem);
 
+static void
+vn_sync_payload_release(struct vn_device *dev, struct vn_sync_payload *payload);
+
 static VkResult
 vn_queue_submission_fix_batch_semaphores(struct vn_queue_submission *submit,
                                          uint32_t batch_index)
@@ -401,6 +404,33 @@ vn_queue_submission_fix_batch_semaphores(struct vn_queue_submission *submit,
       if (payload->type != VN_SYNC_TYPE_IMPORTED_SYNC_FD)
          continue;
 
+      /* Hand the sync_file to the renderer as the HOST semaphore's temporary
+       * payload so the queue submission waits it on the GPU.  The old path
+       * below CPU-blocks in sync_wait until the fd signals before even
+       * submitting — one full frame of added latency per acquire fence
+       * (measured ~3 ms per SurfaceFlinger composite).
+       */
+      struct vn_renderer *renderer = dev->instance->renderer;
+      if (renderer->info.has_semaphore_import && payload->fd >= 0) {
+         /* the import travels over the socket while queue commands travel
+          * over the ring: drain the ring first so an earlier submission
+          * waiting this semaphore cannot consume the new payload
+          */
+         vn_ring_roundtrip(dev->primary_ring);
+         if (renderer->ops.semaphore_import_sync_fd(
+                renderer, dev->base.id, sem->base.id, payload->fd) ==
+             VK_SUCCESS) {
+            /* host owns the wait now; drop our copy and reset the guest-side
+             * payload model (temporary import is consumed by the wait)
+             */
+            vn_sync_payload_release(dev, &sem->temporary);
+            sem->payload = &sem->permanent;
+            continue;
+         }
+         vn_log(dev->instance,
+                "semaphore sync_fd import failed, using CPU wait");
+      }
+
       if (!vn_semaphore_wait_external(dev, sem))
          return VK_ERROR_DEVICE_LOST;
 
@@ -1853,6 +1883,7 @@ vn_create_sync_file(struct vn_device *dev,
       .sync_values = &(const uint64_t){ 1 },
       .sync_count = 1,
       .ring_idx = external_payload->ring_idx,
+      .external_fence = true,
    };
 
    uint32_t local_data[8];
diff --git a/src/virtio/vulkan/vn_renderer.h b/src/virtio/vulkan/vn_renderer.h
index fc5ff2c..d5c1150 100644
--- a/src/virtio/vulkan/vn_renderer.h
+++ b/src/virtio/vulkan/vn_renderer.h
@@ -66,6 +66,11 @@ struct vn_renderer_info {
 
    bool has_dma_buf_import;
    bool has_external_sync;
+   /* renderer can adopt a sync_file fd as a host semaphore's temporary
+    * payload (VCMD_SEMAPHORE_IMPORT_SYNC_FD over vtest), so queue
+    * submissions wait it on the GPU instead of a guest-CPU sync_wait
+    */
+   bool has_semaphore_import;
    bool has_timeline_sync;
    bool has_implicit_fencing;
    bool has_guest_vram;
@@ -103,6 +108,15 @@ struct vn_renderer_submit_batch {
    /* TODO allow NULL when syncs are all binary? */
    const uint64_t *sync_values;
    uint32_t sync_count;
+
+   /* This batch exists to materialize an exportable fence (see
+    * vn_create_sync_file); transports may give its fence a shared-timeline
+    * point for local sync_file export.  Only such fences may be pointed:
+    * DRM timeline points are cumulative (point N waits all points <= N), so
+    * pointing regular submits would chain exported fences to unrelated
+    * work (e.g. buffer-release semaphore waits) and serialize the pipeline.
+    */
+   bool external_fence;
 };
 
 struct vn_renderer_wait {
@@ -128,6 +142,16 @@ struct vn_renderer_ops {
     */
    VkResult (*wait)(struct vn_renderer *renderer,
                     const struct vn_renderer_wait *wait);
+
+   /* import sync_file_fd as the temporary payload of the host semaphore
+    * identified by venus object ids; the fd is NOT consumed.  Synchronous:
+    * on return the import is ordered before later ring submissions.
+    * Optional (see info.has_semaphore_import).
+    */
+   VkResult (*semaphore_import_sync_fd)(struct vn_renderer *renderer,
+                                        uint64_t device_id,
+                                        uint64_t semaphore_id,
+                                        int sync_file_fd);
 };
 
 struct vn_renderer_shmem_ops {
diff --git a/src/virtio/vulkan/vn_renderer_vtest.c b/src/virtio/vulkan/vn_renderer_vtest.c
index 7aaebd2..bfc7184 100644
--- a/src/virtio/vulkan/vn_renderer_vtest.c
+++ b/src/virtio/vulkan/vn_renderer_vtest.c
@@ -10,12 +10,15 @@
 #include <errno.h>
 #include <netinet/in.h>
 #include <poll.h>
+#include <sys/ioctl.h>
 #include <sys/mman.h>
 #include <sys/socket.h>
 #include <sys/types.h>
 #include <sys/un.h>
+#include <time.h>
 #include <unistd.h>
 
+#include "drm-uapi/drm.h"
 #include "drm-uapi/virtgpu_drm.h"
 #include "util/os_file.h"
 #include "util/os_misc.h"
@@ -44,6 +47,11 @@ struct vtest_bo {
 
 struct vtest_sync {
    struct vn_renderer_sync base;
+
+   /* timeline point assigned by the last submit that signals this sync;
+    * 0 = none (fall back to VCMD_SYNC_EXPORT_SYNC_FILE)
+    */
+   uint64_t timeline_point;
 };
 
 struct vtest {
@@ -57,6 +65,19 @@ struct vtest {
    uint32_t protocol_version;
    uint32_t max_timeline_count;
    bool has_venus_sync_fd;
+   bool has_semaphore_import;
+
+   /* Shared DRM timeline syncobj (see docs/fence-timeline-design.md in the
+    * waydroid-nvidia repo): the server materializes each pointed submit's
+    * fence at its timeline point; sync_file export becomes local syncobj
+    * ioctls instead of socket roundtrips.  node_fd is an open render-node fd
+    * received from the server (no /dev/dri access needed in the guest).
+    */
+   struct {
+      int node_fd;         /* -1 = unsupported/disabled */
+      uint32_t handle;     /* timeline syncobj handle on node_fd */
+      uint64_t next_point; /* guarded by sock_mutex */
+   } timeline;
 
    struct {
       uint32_t id;
@@ -532,6 +553,39 @@ vtest_vcmd_resource_import_blob(struct vtest *vtest, uint64_t size, int fd)
    return res_id;
 }
 
+/* on success returns 0 and sets *out_syncobj_fd/*out_node_fd (caller owns) */
+static int
+vtest_vcmd_timeline_get(struct vtest *vtest, int *out_syncobj_fd, int *out_node_fd)
+{
+   uint32_t vtest_hdr[VTEST_HDR_SIZE];
+
+   vtest_hdr[VTEST_CMD_LEN] = 0;
+   vtest_hdr[VTEST_CMD_ID] = VCMD_TIMELINE_GET;
+   vtest_write(vtest, vtest_hdr, sizeof(vtest_hdr));
+
+   vtest_read(vtest, vtest_hdr, sizeof(vtest_hdr));
+   assert(vtest_hdr[VTEST_CMD_ID] == VCMD_TIMELINE_GET);
+
+   uint32_t status;
+   vtest_read(vtest, &status, sizeof(status));
+   if (status)
+      return -ENODEV;
+
+   const int syncobj_fd = vtest_receive_fd(vtest);
+   const int node_fd = vtest_receive_fd(vtest);
+   if (syncobj_fd < 0 || node_fd < 0) {
+      if (syncobj_fd >= 0)
+         close(syncobj_fd);
+      if (node_fd >= 0)
+         close(node_fd);
+      return -ENODEV;
+   }
+
+   *out_syncobj_fd = syncobj_fd;
+   *out_node_fd = node_fd;
+   return 0;
+}
+
 static int
 vtest_vcmd_sync_export_sync_file(struct vtest *vtest,
                                  uint32_t sync_id,
@@ -581,11 +635,31 @@ vtest_vcmd_submit_cmd2(struct vtest *vtest,
    if (!total_size)
       return;
 
+   /* Assign a timeline point so the host materializes this submit's fence
+    * into the shared timeline; recorded on the last sync for local export.
+    * Only external-fence batches may be pointed — timeline points are
+    * cumulative, so pointing regular submits would chain exported fences to
+    * unrelated work (see vn_renderer_submit_batch::external_fence).
+    */
+   uint64_t point = 0;
+   if (vtest->timeline.handle && batch->external_fence && batch->sync_count) {
+      point = ++vtest->timeline.next_point;
+      struct vtest_sync *last =
+         (struct vtest_sync *)batch->syncs[batch->sync_count - 1];
+      last->timeline_point = point;
+   }
+
    uint32_t vtest_hdr[VTEST_HDR_SIZE];
-   vtest_hdr[VTEST_CMD_LEN] = total_size / sizeof(uint32_t);
-   vtest_hdr[VTEST_CMD_ID] = VCMD_SUBMIT_CMD2;
+   vtest_hdr[VTEST_CMD_LEN] =
+      total_size / sizeof(uint32_t) + (point ? 2 : 0);
+   vtest_hdr[VTEST_CMD_ID] = point ? VCMD_SUBMIT_CMD2_TIMELINE : VCMD_SUBMIT_CMD2;
    vtest_write(vtest, vtest_hdr, sizeof(vtest_hdr));
 
+   if (point) {
+      const uint32_t point_buf[2] = { (uint32_t)point, (uint32_t)(point >> 32) };
+      vtest_write(vtest, point_buf, sizeof(point_buf));
+   }
+
    /* write batch count and batch headers */
    const uint32_t batch_count = 1;
    size_t sync_offset = header_size + batch->cs_size;
@@ -658,6 +732,59 @@ vtest_sync_reset(struct vn_renderer *renderer,
    return vtest_sync_write(renderer, sync, initial_val);
 }
 
+/* Export a sync_file of a materialized timeline point with local syncobj
+ * ioctls only — zero socket traffic.  Returns the fd, or < -1 to make the
+ * caller fall back to the socket export.
+ */
+static int
+vtest_timeline_export_sync_file(struct vtest *vtest, uint64_t point)
+{
+   const int node_fd = vtest->timeline.node_fd;
+
+   /* wait for the host to materialize the point (QueueSubmit + import);
+    * the real GPU wait stays with the consumer of the sync_file
+    */
+   struct timespec ts;
+   clock_gettime(CLOCK_MONOTONIC, &ts);
+   struct drm_syncobj_timeline_wait wait = {
+      .handles = (uintptr_t)&vtest->timeline.handle,
+      .points = (uintptr_t)&point,
+      .timeout_nsec = (int64_t)ts.tv_sec * 1000000000 + ts.tv_nsec +
+                      1000ll * 1000 * 1000,
+      .count_handles = 1,
+      /* WAIT_FOR_SUBMIT is required to block on a not-yet-materialized
+       * point (without it the kernel returns -EINVAL immediately);
+       * WAIT_AVAILABLE makes the wait end at materialization instead of
+       * signal
+       */
+      .flags = DRM_SYNCOBJ_WAIT_FLAGS_WAIT_FOR_SUBMIT |
+               DRM_SYNCOBJ_WAIT_FLAGS_WAIT_AVAILABLE,
+   };
+   if (ioctl(node_fd, DRM_IOCTL_SYNCOBJ_TIMELINE_WAIT, &wait))
+      return -ENOTSUP;
+
+   struct drm_syncobj_create create = { 0 };
+   if (ioctl(node_fd, DRM_IOCTL_SYNCOBJ_CREATE, &create))
+      return -ENOTSUP;
+
+   struct drm_syncobj_transfer xfer = {
+      .src_handle = vtest->timeline.handle,
+      .dst_handle = create.handle,
+      .src_point = point,
+   };
+   struct drm_syncobj_handle export = {
+      .handle = create.handle,
+      .flags = DRM_SYNCOBJ_HANDLE_TO_FD_FLAGS_EXPORT_SYNC_FILE,
+   };
+   const bool ok = !ioctl(node_fd, DRM_IOCTL_SYNCOBJ_TRANSFER, &xfer) &&
+                   !ioctl(node_fd, DRM_IOCTL_SYNCOBJ_HANDLE_TO_FD, &export);
+
+   struct drm_syncobj_destroy destroy = { .handle = create.handle };
+   ioctl(node_fd, DRM_IOCTL_SYNCOBJ_DESTROY, &destroy);
+
+   return ok ? export.fd : -ENOTSUP;
+}
+
 static int
 vtest_sync_export_syncobj(struct vn_renderer *renderer,
                           struct vn_renderer_sync *_sync,
@@ -670,6 +797,14 @@ vtest_sync_export_syncobj(struct vn_renderer *renderer,
    if (!sync_file || !vtest->has_venus_sync_fd)
       return -ENOTSUP;
 
+   if (vtest->timeline.handle && sync->timeline_point) {
+      const int fd =
+         vtest_timeline_export_sync_file(vtest, sync->timeline_point);
+      if (fd >= 0)
+         return fd;
+      vn_log(vtest->instance, "timeline export failed, socket fallback");
+   }
+
    /* Resolve the (binary) sync's signal point to the pending submit's
     * VkFence on the server and export a sync_file from it.  -1 means the
     * point already retired (already-signaled sync_file convention).
@@ -1032,6 +1167,44 @@ vtest_submit(struct vn_renderer *renderer,
    return VK_SUCCESS;
 }
 
+/* hand a sync_file to the server to become the semaphore's temporary
+ * payload; the server dups the fd (SCM_RIGHTS) so the caller keeps
+ * ownership.  The reply orders the import before later ring submissions.
+ */
+static VkResult
+vtest_semaphore_import_sync_fd(struct vn_renderer *renderer,
+                               uint64_t device_id,
+                               uint64_t semaphore_id,
+                               int sync_file_fd)
+{
+   struct vtest *vtest = (struct vtest *)renderer;
+   uint32_t vtest_hdr[VTEST_HDR_SIZE];
+   const uint32_t cmd[4] = {
+      (uint32_t)device_id,
+      (uint32_t)(device_id >> 32),
+      (uint32_t)semaphore_id,
+      (uint32_t)(semaphore_id >> 32),
+   };
+
+   mtx_lock(&vtest->sock_mutex);
+   vtest_hdr[VTEST_CMD_LEN] = ARRAY_SIZE(cmd);
+   vtest_hdr[VTEST_CMD_ID] = VCMD_SEMAPHORE_IMPORT_SYNC_FD;
+   vtest_write(vtest, vtest_hdr, sizeof(vtest_hdr));
+   vtest_write(vtest, cmd, sizeof(cmd));
+   if (vtest_send_fd(vtest, sync_file_fd)) {
+      mtx_unlock(&vtest->sock_mutex);
+      return VK_ERROR_DEVICE_LOST;
+   }
+
+   vtest_read(vtest, vtest_hdr, sizeof(vtest_hdr));
+   assert(vtest_hdr[VTEST_CMD_ID] == VCMD_SEMAPHORE_IMPORT_SYNC_FD);
+   uint32_t status;
+   vtest_read(vtest, &status, sizeof(status));
+   mtx_unlock(&vtest->sock_mutex);
+
+   return status ? VK_ERROR_INVALID_EXTERNAL_HANDLE : VK_SUCCESS;
+}
+
 static void
 vtest_init_renderer_info(struct vtest *vtest)
 {
@@ -1042,6 +1215,7 @@ vtest_init_renderer_info(struct vtest *vtest)
 
    info->has_dma_buf_import = vtest->has_venus_sync_fd;
    info->has_external_sync = vtest->has_venus_sync_fd;
+   info->has_semaphore_import = vtest->has_semaphore_import;
    info->has_implicit_fencing = false;
 
    const struct virgl_renderer_capset_venus *capset = &vtest->capset.data;
@@ -1078,6 +1252,12 @@ vtest_destroy(struct vn_renderer *renderer,
       close(vtest->sock_fd);
    }
 
+   if (vtest->timeline.node_fd >= 0) {
+      struct drm_syncobj_destroy destroy = { .handle = vtest->timeline.handle };
+      ioctl(vtest->timeline.node_fd, DRM_IOCTL_SYNCOBJ_DESTROY, &destroy);
+      close(vtest->timeline.node_fd);
+   }
+
    mtx_destroy(&vtest->sock_mutex);
    util_sparse_array_finish(&vtest->shmem_array);
    util_sparse_array_finish(&vtest->bo_array);
@@ -1172,11 +1352,39 @@ vtest_init(struct vtest *vtest)
 
    vtest_vcmd_context_init(vtest, vtest->capset.id);
 
+   /* fetch the shared fence timeline (syncobj fd + render-node fd); on any
+    * failure stay on the socket sync_file export path
+    */
+   vtest->timeline.node_fd = -1;
+   if (vtest->has_venus_sync_fd &&
+       vtest_vcmd_get_param(vtest, VCMD_PARAM_HAS_TIMELINE_FENCE) &&
+       !os_get_option("VTEST_NO_TIMELINE_FENCE")) {
+      int syncobj_fd, node_fd;
+      if (!vtest_vcmd_timeline_get(vtest, &syncobj_fd, &node_fd)) {
+         struct drm_syncobj_handle import = { .fd = syncobj_fd };
+         if (!ioctl(node_fd, DRM_IOCTL_SYNCOBJ_FD_TO_HANDLE, &import)) {
+            vtest->timeline.node_fd = node_fd;
+            vtest->timeline.handle = import.handle;
+         } else {
+            close(node_fd);
+         }
+         close(syncobj_fd);
+      }
+      if (vtest->timeline.node_fd < 0)
+         vn_log(vtest->instance, "fence timeline unavailable, using socket export");
+   }
+
+   vtest->has_semaphore_import =
+      vtest->has_venus_sync_fd &&
+      vtest_vcmd_get_param(vtest, VCMD_PARAM_HAS_SEMAPHORE_IMPORT) &&
+      !os_get_option("VTEST_NO_SEMAPHORE_IMPORT");
+
    vtest_init_renderer_info(vtest);
 
    vtest->base.ops.destroy = vtest_destroy;
    vtest->base.ops.submit = vtest_submit;
    vtest->base.ops.wait = vtest_wait;
+   vtest->base.ops.semaphore_import_sync_fd = vtest_semaphore_import_sync_fd;
 
    vtest->base.shmem_ops.create = vtest_shmem_create;
    vtest->base.shmem_ops.destroy = vtest_shmem_destroy;
diff --git a/src/vulkan/runtime/meson.build b/src/vulkan/runtime/meson.build
index 7da99c8..05cee35 100644
--- a/src/vulkan/runtime/meson.build
+++ b/src/vulkan/runtime/meson.build
@@ -170,6 +170,23 @@ vk_format_info = custom_target(
   ],
 )
 
+# ASTC decode emulation is pure Vulkan API + prebuilt SPIR-V (no NIR); the
+# lite-runtime venus driver uses it to emulate ASTC on hosts without it.
+if prog_glslang.found()
+  astc_spv_h = custom_target(
+    'astc_spv.h',
+    input : astc_decoder_glsl_file,
+    output : 'astc_spv.h',
+    command : [
+      prog_glslang, '-V', '-S', 'comp', '-x', '-o', '@OUTPUT@', '@INPUT@',
+      glslang_quiet, glslang_depfile,
+    ],
+    depfile : 'astc_spv.h.d',
+  )
+  vulkan_lite_runtime_files += files('vk_texcompress_astc.c')
+  vulkan_lite_runtime_files += astc_spv_h
+endif
+
 vulkan_lite_runtime_files += [
   vk_cmd_enqueue_entrypoints,
   vk_cmd_queue,
@@ -285,17 +302,6 @@ if prog_glslang.found()
     vulkan_runtime_files += files('vk_acceleration_structure.c')
     vulkan_runtime_files += bvh_spv
   endif
-  vulkan_runtime_files += files('vk_texcompress_astc.c')
-  vulkan_runtime_files += custom_target(
-    'astc_spv.h',
-    input : astc_decoder_glsl_file,
-    output : 'astc_spv.h',
-    command : [
-      prog_glslang, '-V', '-S', 'comp', '-x', '-o', '@OUTPUT@', '@INPUT@',
-      glslang_quiet, glslang_depfile,
-    ],
-    depfile : 'astc_spv.h.d',
-  )
 endif
 
 libvulkan_runtime = static_library(
diff --git a/src/vulkan/runtime/vk_android.c b/src/vulkan/runtime/vk_android.c
index fd9ee5d..f773a51 100644
--- a/src/vulkan/runtime/vk_android.c
+++ b/src/vulkan/runtime/vk_android.c
@@ -145,8 +145,10 @@ vk_gralloc_to_drm_explicit_layout(
    struct u_gralloc *u_gralloc = vk_android_get_ugralloc();
    assert(u_gralloc);
 
-   if (u_gralloc_get_buffer_basic_info(u_gralloc, in_hnd, &info) != 0)
+   if (u_gralloc_get_buffer_basic_info(u_gralloc, in_hnd, &info) != 0) {
+      mesa_loge("gralloc_to_layout: basic_info FAILED");
       return VK_ERROR_INVALID_EXTERNAL_HANDLE;
+   }
 
    if (info.num_planes > max_planes)
       return VK_ERROR_INVALID_EXTERNAL_HANDLE;
@@ -884,15 +886,21 @@ get_ahb_buffer_format_properties2(
    /* "Buffer must be a valid Android hardware buffer object with at least
     * one of the AHARDWAREBUFFER_USAGE_GPU_* usage flags."
     */
-   if (!gpu_usage)
+   if (!gpu_usage) {
+      mesa_loge("NVWD: ahb_props: no GPU usage (usage 0x%llx fmt %u %ux%u)",
+                (unsigned long long)desc.usage, desc.format, desc.width,
+                desc.height);
       return VK_ERROR_INVALID_EXTERNAL_HANDLE;
+   }
 
    /* No known gralloc implementations currently allocate with a
     * layers > 1. So return an error if we happen to get one since
     * the rest of mesa won't handle it properly.
     */
-   if (desc.layers > 1)
+   if (desc.layers > 1) {
+      mesa_loge("NVWD: ahb_props: layers %u > 1", desc.layers);
       return VK_ERROR_INVALID_EXTERNAL_HANDLE_KHR;
+   }
 
    /* Fill properties fields based on description. */
    VkAndroidHardwareBufferFormatProperties2ANDROID *p = pProperties;
@@ -1096,8 +1104,11 @@ vk_common_GetAndroidHardwareBufferPropertiesANDROID(
    result = device->dispatch_table.GetMemoryFdPropertiesKHR(
       device_h, VK_EXTERNAL_MEMORY_HANDLE_TYPE_DMA_BUF_BIT_EXT, handle->data[0],
       &fd_props);
-   if (result != VK_SUCCESS)
+   if (result != VK_SUCCESS) {
+      mesa_loge("GetAHBProps: GetMemoryFdPropertiesKHR failed %d", result);
       return result;
+   }
+
 
    pProperties->memoryTypeBits = fd_props.memoryTypeBits;
 
@@ -1146,6 +1157,8 @@ vk_android_get_ahb_image_properties(
    }
 
    if (!vk_ahb_probe_format(info->format, info->flags, info->usage)) {
+      mesa_loge("AHB probe REJECTED: format %u flags 0x%x usage 0x%x",
+                info->format, info->flags, info->usage);
       return vk_errorf(
          pdevice, VK_ERROR_FORMAT_NOT_SUPPORTED,
          "format (%u) flags (0x%x) usage (0x%x) unsupported for AHB",
